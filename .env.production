# 🚀 BillGenie Restaurant API - Production Environment Configuration
# Copy this file to .env on your production server and update values

# ================================================
# DATABASE CONFIGURATION
# ================================================
# For DigitalOcean/Heroku: They provide DATABASE_URL automatically
# For manual setup: Use your cloud database credentials
DATABASE_URL=postgresql://user:password@your-cloud-db-host:5432/restaurant_db
DATABASE_HOST=your-cloud-db-host
DATABASE_PORT=5432
DATABASE_USER=your_db_user
DATABASE_PASSWORD=your_secure_db_password_here
DATABASE_NAME=restaurant_db
DATABASE_DRIVER=postgres

# ================================================
# SERVER CONFIGURATION
# ================================================
SERVER_PORT=3000
SERVER_ENV=production
API_BASE_URL=https://your-domain.com
LOG_LEVEL=info

# ================================================
# JWT AUTHENTICATION - CHANGE THESE IMMEDIATELY!
# ================================================
# Generate new secrets for production:
# Linux/Mac: openssl rand -base64 32
# PowerShell: [System.Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
JWT_SECRET=your-unique-production-jwt-secret-change-this-now
JWT_EXPIRY=24h
REFRESH_TOKEN_EXPIRY=7d
REFRESH_JWT_SECRET=your-unique-production-refresh-secret-change-this-now

# ================================================
# WEBSOCKET CONFIGURATION
# ================================================
WEBSOCKET_PORT=3000
WEBSOCKET_READ_BUFFER=1024
WEBSOCKET_WRITE_BUFFER=1024

# ================================================
# PAYMENT GATEWAY (Razorpay)
# ================================================
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# ================================================
# CORS CONFIGURATION
# ================================================
# Add your frontend domain(s) here
# Example: https://app.billgenie.com,https://staging-app.billgenie.com
CORS_ALLOWED_ORIGINS=https://your-app-domain.com

# ================================================
# FEATURES
# ================================================
ENABLE_PAYMENT=true
ENABLE_WEBSOCKET=true
ENABLE_LOGGING=true

# ================================================
# EMAIL CONFIGURATION (Optional - for notifications)
# ================================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-email-app-password

# ================================================
# NOTES FOR CLOUD DEPLOYMENT
# ================================================
# 1. Never commit this file to git - add .env.production to .gitignore
# 2. Use your cloud provider's secret management:
#    - DigitalOcean: App Platform → Environment Variables
#    - Heroku: heroku config:set KEY=VALUE
#    - AWS: Systems Manager Parameter Store
# 3. Always use HTTPS in production (sslmode=require for database)
# 4. Regularly rotate JWT secrets
# 5. Use strong database passwords (20+ characters, mix of upper/lower/numbers/symbols)
# 6. Monitor server logs for errors and security issues
